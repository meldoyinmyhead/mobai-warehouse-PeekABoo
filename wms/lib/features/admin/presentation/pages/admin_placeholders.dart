import 'package:flutter/material.dart';

class AccessLogsScreen extends StatelessWidget {
  const AccessLogsScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text("Access Logs")));
}

class ExportReportsScreen extends StatelessWidget {
  const ExportReportsScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text("Export Reports")));
}

class AIPerformanceScreen extends StatelessWidget {
  const AIPerformanceScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text("AI Performance")));
}
